task: hallucination_analysis
dataset:
  name: M-HalDetect
  root: data/mhaldetect
  split: test
model:
  name: InstructBLIP-span-detector
  checkpoint: checkpoints/instructblip_mhaldetect
target_score: span_hallucination_logit
evidence:
  visual: sam_masks
  textual: response_spans
  relation: absence_of_support
pmesa: {integration_steps: 50, paths: 6, budget: 5, seed: 0}
evaluation: [insertion_auc, deletion_auc, sufficiency, comprehensiveness, span_f1, stability]

