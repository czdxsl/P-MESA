task: vqa
dataset:
  name: VQA-X
  root: data/vqa_x
  split: test
model:
  name: ALBEF-VQA
  checkpoint: checkpoints/albef_vqa.pth
target_score: predicted_answer_logit
evidence:
  visual: sam_masks
  textual: semantic_phrases
  relation: grounding
pmesa: {integration_steps: 50, paths: 6, budget: 5, seed: 0}
evaluation: [insertion_auc, deletion_auc, sufficiency, comprehensiveness, pointing_game, stability]

