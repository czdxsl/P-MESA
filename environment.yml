name: pmesa
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch>=2.0
  - torchvision>=0.15
  - pytorch-cuda=11.8
  - numpy>=1.24
  - pillow>=9
  - pyyaml>=6
  - pip
  - pip:
      - reportlab>=4
      - transformers>=4.35
      - pytest>=7
